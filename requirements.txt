# Core Flask dependencies
Flask==3.0.0
Werkzeug==3.0.1
Jinja2==3.1.2
MarkupSafe==2.1.3
itsdangerous==2.1.2
click==8.1.7
blinker==1.7.0

# Markdown processing
markdown==3.5.1
bleach==6.1.0
Pygments==2.17.2

# Data processing
numpy>=1.24.0
requests>=2.31.0
tqdm>=4.65.0

# Machine Learning dependencies (for model preparation)
# Note: These are large dependencies. Install only if you need to prepare models.
# For production deployment, you only need the ONNX runtime.

# Model preparation (comment out if only running the app)
torch>=2.0.0
transformers>=4.35.0
onnx>=1.15.0
onnxruntime>=1.16.0

# Optional: For optimized model conversion (may have installation issues)
# optimum[exporters]>=1.16.0

# Optional: Additional ML tools
# pandas>=2.0.0
# scikit-learn>=1.3.0
# huggingface-hub>=0.19.0

# Testing (optional)
# pytest>=7.4.0
# pytest-cov>=4.1.0